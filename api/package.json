{
  "name": "ohm-api",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run build && npm run start",
    "dev:tsx": "node --dns-result-order=ipv4first node_modules/.bin/tsx --tsconfig tsconfig.json src/index.ts",
    "dev:watch": "node --dns-result-order=ipv4first node_modules/.bin/tsx watch --tsconfig tsconfig.json src/index.ts",
    "typecheck": "node node_modules/typescript/bin/tsc --noEmit --pretty false",
    "build": "node --dns-result-order=ipv4first scripts/build.mjs",
    "start": "node --dns-result-order=ipv4first dist/index.js",
    "db:migrate": "node --dns-result-order=ipv4first -r tsx/cjs src/db/migrate.ts",
    "db:ping": "node --dns-result-order=ipv4first -r tsx/cjs scripts/ping-db.ts",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "learning:daily": "tsx src/learning/cron.ts daily",
    "learning:weekly": "tsx src/learning/cron.ts weekly"
  },
  "dependencies": {
    "@fal-ai/client": "^1.2.0",
    "@fastify/cors": "^10.0.0",
    "@fastify/jwt": "^9.0.0",
    "@fastify/rate-limit": "^10.0.0",
    "@fastify/websocket": "^11.0.0",
    "dotenv": "^16.4.0",
    "drizzle-orm": "^0.38.0",
    "fastify": "^5.0.0",
    "node-cron": "^4.2.1",
    "pg": "^8.13.0",
    "postgres": "^3.4.5",
    "zod": "^3.23.0"
  },
  "optionalDependencies": {
    "@aws-sdk/client-s3": "^3.700.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/node-cron": "^3.0.11",
    "@types/pg": "^8.11.0",
    "drizzle-kit": "^0.30.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
