Phase 2 embedding service.

The feed should work as retrieval, not live generation.

Required embedding paths:

- raw text surface embedding
- primary resonance embedding derived from the thought processor output

Cold start:

- use internal profile prompt text only when the user has no processed thoughts
- once the first thought exists, prioritize thought-derived signal
