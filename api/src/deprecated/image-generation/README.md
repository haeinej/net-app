# Deprecated: AI Image Generation (fal.ai)

Archived March 11, 2026. Replaced with the client-side mesh overlay system.

This code used Flux + IP-Adapter via fal.ai to generate AI images per thought and crossing.
It is kept here for potential future use such as premium image features or model experiments.

To restore: move these files back to `api/src/image`, re-enable the commented `FAL_KEY` env var, and reconnect the routes that previously imported the image service.
