Phase 3 thought processing pipeline.

Input:

- sentence
- context

LLM output:

- tensions
- domains
- openness
- abstraction
- resonance phrases

Runtime compatibility:

- store raw text embedding as `surface_embedding`
- store primary resonance embedding in the existing `question_embedding` column
- treat `quality_score` as an openness-weighted signal

Failure handling:

- retry once
- if still failing, enqueue for reprocessing
